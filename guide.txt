in the terminal :

cd ~/quicklisp/local-projects/lisp-webapp
sbcl

in the sbcl :

(ql:quickload "lisp-webapp" :force t)
(lisp-webapp:start-app)

to stop server :

Ctrl + D or (lisp-webapp:stop-app)

to push :

git push -u origin main
